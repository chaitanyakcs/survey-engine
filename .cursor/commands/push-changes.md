Look at the diff of changes.. classify into different changes made and come up with commit message 

If multiple tasks changed the same file, use judgement to assign the change to one of the commits.

Run critical test cases and if they pass, push to Github. 